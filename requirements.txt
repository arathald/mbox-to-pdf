# Core dependencies
xhtml2pdf>=0.2.11       # Pure Python HTML→PDF (no native deps, easy distribution)
pycairo==1.29.0         # Transitive dependency with good wheel support

# Attachment handling
openpyxl>=3.1.0         # Excel (.xlsx) files
python-docx>=0.8.11     # Word (.docx) files
Pillow>=9.0.0           # Image processing and embedding
bleach>=5.0.0           # HTML sanitization

# Testing
pytest>=9.0.0
pytest-cov>=7.0.0

# Development
black>=24.3.0
flake8>=7.3.0
mypy>=1.19.0

# For building standalone executables
pyinstaller>=6.17.0      # Supports namespace packages (jaraco/setuptools)
